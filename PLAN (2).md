# National_Test 13 点端到端 SAC 重构计划

## 结论

- 现场已复现：第 3 点完成后进入 `Point: 3`，第一次 `PLANNING_HOLD` 后距离由 3.71 m 降到 2.17 m；随后中途重规划触发，下一周期变成 `PLANNING_DEFERRED` 并输出零油门。
- 根因是第 4 段将 5 个动作裁剪成仅 3 个唯一动作，并以固定 seed 从偏离理想轨迹的实测船态重新规划；该特殊分支失败后绕过通用恢复，异常细节又被吞掉。[fixed_route.py](<G:/NavAlg - 副本/usvlib4ros/planning/fixed_route.py:1468>) [fixed_map_runtime.py](<G:/NavAlg - 副本/usvlib4ros/navigation/fixed_map_runtime.py:1269>)
- 失败计数目前只记录、不终止，因而可持续零输出至 600 秒超时。[fixed_map_service.py](<G:/NavAlg - 副本/usvlib4ros/navigation/fixed_map_service.py:607>)
- 现有 6 个相关测试节点全部通过，说明测试只覆盖理想到达姿态，没有覆盖 Unity 偏航后的状态包络。
- 当前 0.1 油门对应模型速度约 0.127 m/s；主办方说明低于 1 m/s 时推力会明显降低，因此现有低速特判还存在舵效不足风险。[主办方操作手册](https://spaitlab.github.io/Maritime-Intelligent-Navigation-2026/%E6%93%8D%E4%BD%9C%E6%89%8B%E5%86%8C/)

## 核心改造

- 保持 `usvlib4ros/main.py` 字节不变，并增加哈希守卫测试。
- RRT* 降为离线工具，生成与地图哈希、13 个原始航点绑定的 `fixed-route-corridor-v1` 冻结参考走廊；中间引导点不参与比赛航点计数。
- 运行时删除在线规划、`PLANNING_HOLD/DEFERRED`、3→5 点专属油门舵角裁剪和反向急刹。航点切换只更新目标索引，不停车、不重规划，并保留 GRU 隐状态。
- 建立唯一的纯 Python 状态转换引擎，供 Unity 运行与离线训练共用：
  `输入校验 → 航点推进 → V3观测 → 执行器可达候选 → 安全mask → SAC选动作 → 最终安全复核 → NavigationStatus输出`。
- 新建 `local-waypoint-observation-v3`：包含 72 路雷达及有效位、数据新鲜度、船速/角速度/实际油门舵角、当前与下一航点的船体坐标特征、任务进度、走廊误差/航向/进度、地图净空和 5 位安全 mask。
- 保留离散循环 SAC，但升级为 `five-calibrated-controls-v3`：硬左、软左、直行、软右、硬右必须在百分比取整后仍互不重复，并通过 Unity 双向舵效标定；禁止任何点位再次裁剪成重复动作。
- 新 checkpoint 使用 `national-test-sac-checkpoint-v6`，随机初始化，不继承 v37 权重、Q 值或回放；旧 checkpoint 和现场日志仅作历史证据保留。
- 恢复不可学习的安全底线：地图额外净空 0.2 m、激光急停 0.6 m。冻结走廊必须在航点容差内满足底线，不得再用零净空 manifest 绕过。
- 执行器候选先经过转换约束再做安全预测：极左/极右不能直接换向，须经过直行过渡；失联、急停和任务结束可立即归零。奖励改用实际控制幅值与变化率，不再用 `abs(action-2)` 这种动作编号惩罚。
- `NavigationStatus` 成为唯一控制输出；`DeviceStatus` 保持只读反馈。删除向设备状态话题自发布、自回读的 `DeviceActionBridge`，并记录策略动作、安全 mask、最终百分比命令及设备回读。

## 自训练与清理

- 训练采用门控式、最多 1000 个训练回合：
  100 局离线训练块 → 20 局确定性评估；达到 20/20 后运行 5 局 Unity 适应，再做 5 局 Unity 验收。失败数据回流后进入下一离线块，1000 局仍未达标则明确停止为 `TRAINING_GATE_FAILED`。
- 离线环境从冻结走廊各段扰动出生姿态并随机化已标定动力学；奖励覆盖航点/走廊进度、13 点完成、净空、控制平滑、碰撞、超时和安全截断。
- 连续 10 个新鲜控制周期无安全动作时归零并截断本局，不再等待 600 秒；输入陈旧只安全停车，不误记为策略失败。
- 操作者结束训练时保存带 `operator_truncated` 标记的安全前缀和遥测，不给终止惩罚，也不计入晋级成绩。
- 删除整目录 `usvlib4ros（1）`、旧 PPO 权重/脚本、旧自动驾驶服务链、不可达 vehicle/rosbag 辅助代码、设备双输出桥和改造后失去引用的特殊规划代码；Git 历史作为恢复来源。
- 保留 `DQN_NAV` 兼容包装供不可修改的 `main.py` 使用；移除旧 `USVAutoNavigationService` 公共导出，并把 `setup.py` 打包范围显式限定为当前 `usvlib4ros`。
- 审核全部 11 个受版本控制的 Markdown；删除已失效的 v35–v37 补丁式 TDD 报告，更新入口说明、自训练状态、架构边界和验证证据。

## 定向测试与验收

- 不跑全量回归，只运行相关测试文件中的精确节点：端到端状态转换、SAC/自训练、入口兼容、冻结走廊和执行器输出。
- 增加以下场景：
  - 13 个航点依次推进且无规划状态，航点切换不重置 GRU。
  - 3→4、5→6、11→12 紧窄段覆盖位置、航向、速度和角速度扰动包络。
  - 5 个动作取整后唯一、左右舵效成立、换向过渡和平滑约束正确。
  - 无安全动作 10 周期后截断；陈旧输入、结束和异常始终归零。
  - `DeviceStatus` 无发布，`NavigationStatus` 是唯一控制通道。
  - 新旧 checkpoint、自训练状态和回放 schema 严格不兼容。
  - 打包结果不包含副本、PPO 或旧服务链。
- 控制周期不得再包含规划调用，单周期上限 100 ms；遥测必须能区分策略提议、安全干预、最终命令和设备反馈。
- 实施时需要你两次打开 Unity：
  1. 先做短时油门/舵角标定和单输出通道确认。
  2. 训练完成后执行连续 5 次正式验收。
- 比赛默认模型的唯一晋级条件：Unity 连续 5/5 完成全部 13 点，零碰撞、零超时、零无安全动作，结束后持续零输出。

## 既定边界

- 仅支持固定 `National_Test` 地图、已知静态障碍和下方出生点。
- 不修改 `main.py`，不迁移旧模型，不削弱硬安全底线，不引入动态避碰或随机地图。
- 不删除历史 checkpoint、日志和可追溯证据；仅清理代码、打包结构及明确失效的活动文档。

## 修改要求

- 不保留向后兼容。过时的直接删，别加兼容层、别写migration、别留fallback。
- 选能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。
- 系统分层长。先跑通一个最小的端到端版本，再往上加东西。绝不为了未完成的复杂度拆掉能跑的东西。
- 组件保持模块化，关注点分离。
- 优先用成熟的、有人维护的库。没有明确理由别自己重写。
- 先翻项目里已有的依赖能做什么，再考虑加新包或自己写。别上来就假设库里没有。
- 架构决策往长了做。不接受"先这样以后再换"的临时方案。
- 先看成熟产品怎么解决同一个问题，用已验证的模式，别从零发明。
- 不要再写那么多的测试文件，只运行必要测试。除非我有指令，否则不准运行全量测试。
