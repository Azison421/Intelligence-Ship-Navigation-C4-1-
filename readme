# 旧打包说明（非当前交付入口）

当前海航赛版本只验证从仓库源码目录运行：

```powershell
python -m pip install -r requirements.txt
python usvlib4ros/main.py --help
```

不要据此文件直接生成比赛 wheel。现有 `setup.py` 的 `install_requires` 为空，且未
声明 `usvlib4ros/mapping/data/*.json` 等运行资产；生成的 wheel 即使可以安装，也
不能保证导航入口可启动。完成以下事项前，wheel／sdist／PyInstaller 均视为未支持：

1. 在打包元数据中声明 `requirements.txt` 对应的运行依赖；
2. 收入地图 sidecar、live profile 和所需 package data；
3. 明确 checkpoint 与 manifest 的外部交付位置；
4. 在干净环境执行安装后 preflight 和样例入口兼容测试。

当前权威运行说明见 `README.md`，入口契约见 `usvlib4ros_main_README.md`。
