# National_Test SAC 自训练与自动迭代方案

> 实施状态（2026-08-04）：代码、聚焦离线测试和项目 Markdown 同步已完成；未运行
> 全量回归，未启动或操作 Unity。真实 Unity 基线、探索、验收和自动晋级仍待用户运行。

## Summary

- 无参数运行 `main.py` 后，Unity“开始训练”按钮启动可中断、可续训的混合训练。
- 从当前 v37 零净空保守候选热启动；首阶段累计训练 1000 轮，后续每次重新开始自动增加 1000 轮目标。
- 每代包含 95 轮 Python 离线训练、5 轮 Unity 安全探索；另运行 20 轮离线评估和 5 轮独立 Unity 确定性验收。
- Informed RRT*、现有 SAC 奖励、13 点顺序、5000 步／600 秒限制不变。

## Implementation Changes

- 新增自训练协调器和持久化会话状态。首次训练前用原始 v37 额外运行 5 次确定性 Unity 基线；之后连续执行 10 代，达到 1000 轮后零控制等待。点击结束时丢弃当前半回合，保留所有完整回合，下次从本代未完成回合继续。
- 离线训练器改为读取候选 profile：额外净空和激光提前停止均为 0，第 3→4 段硬限制 `0.1/±0.1`，第 4→5 段硬限制 `0.1/0.12`。离线和 Unity 共用现有 reward 计算；碰撞记录为安全截断负样本并立即 Reset。
- SAC 可在全路线选择任一通过预测安全掩码的动作，不再被最小干预逻辑固定回名义动作；特殊路段生成有差异但不突破上述硬上限的 5 个候选。0.4 m 船体、碰撞预测、数据新鲜度检查和安全监督最终否决权保留。
- 每个离线回合执行 16 次 SAC 更新；5 个 Unity 探索回合结束后执行 80 次混合更新。批量大小保持 8，其中 6 条来自离线回放、2 条来自 Unity 回放；首批没有 Unity 数据时使用纯离线批次。离线／Unity 回放分别限长为 32／20 个完整回合。
- 训练状态在每个完整回合后原子保存，包含网络、目标网络、优化器、温度参数、训练步数、随机数状态、双回放池、代次和阶段进度。训练限制为 8 个 CPU 线程；异常、非有限 loss 或状态损坏时发布零控制并恢复最近完整冠军，不静默重建。
- 每代生成不可覆盖的 v5 checkpoint，记录父模型哈希、训练谱系、profile、回合数和评估证据。旧 v4 checkpoint 继续兼容且显式路径永远固定。
- 自动晋级要求：离线评估 20/20 完成且无安全停止／超时；独立 Unity 验收 5/5 完成 13 点、零碰撞、零激光急停、零未恢复不安全事件。通过后先比较完成率；完成率相同时，Unity 中位总步数至少改善 2% 才晋级。
- 晋级后原子更新 `national_test_sac_active.json`，无参数的 `unity_test`、`offline_validation` 和 `live` 均在下一回合加载新冠军；同时解除新冠军 0/0 安全 profile 和保守机动 profile 的 Unity-only 限制。显式 `--checkpoint PATH` 始终锁定，不跟随冠军。
- 某代探索发生碰撞时，完成该代剩余训练以保留纠错数据，但该代禁止晋级；代末恢复冠军网络与优化器，保留回放数据继续下一代。
- 新增 `--validate-only`。无参数入口默认自训练；显式 checkpoint 或显式 `live` 只做确定性运行，不启动梯度更新。
- 保留现有训练日志，新增 `self_training_episodes.csv`、`self_training_generations.csv` 及 reward、成功率、总步数、actor/critic loss SVG。Unity 界面继续使用原字段：`E` 为累计训练轮数、`Step` 为当前步、`Score` 为累计 reward、`Loss` 为最新 critic loss、`MaxE` 为当前累计目标。

## Focused Test Plan

- 先写失败测试，证明当前 Unity 点击不会调用 SAC 更新、checkpoint 哈希不变化，再接入训练闭环。
- 验证零净空和保守段 profile 同时用于离线／Unity；全路线候选存在差异且不突破路段硬上限，预测不安全动作仍被拒绝。
- 验证真实 Unity 样本形成连续 RNN transition，重复／过期传感器样本不会重复写入；碰撞立即零控制、Reset、记录终止负样本并禁止晋级。
- 验证 95/5/20/5 状态机、6:2 混合采样、每回合保存、中断续训、1000 轮目标扩展以及 checkpoint 权重和 `training_step` 实际变化。
- 验证双门槛、2% 冠军比较、失败回滚、profile 自动解锁、活动指针原子切换和显式 checkpoint 固定行为。
- 验证新增 CSV/SVG 可跨点击追加且旧报告格式不变。
- 只运行相关离线测试、语法、Markdown 链接和差异检查；不运行全量回归，不启动或操作 Unity。同步审查全部项目 Markdown，并新增本功能的 TDD 证据报告。

## Locked Assumptions

- 所有训练和自动 `live` 晋级只适用于哈希匹配的固定北湖／National_Test 地图及当前标定。
- Unity 训练回合允许安全掩码内随机探索；验收和冠军运行始终使用确定性策略。
- 半回合不跨 Unity Reset 续接；`PLANNING_DEFERRED` 继续零控制重试，只有既有超时、碰撞、停止请求等条件结束回合。
- 自动替换只发生在代次边界，绝不在正在执行的回合中热换权重。
