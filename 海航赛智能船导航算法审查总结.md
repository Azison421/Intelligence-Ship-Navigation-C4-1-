# National_Test 导航算法审查总结

日期：2026-08-06

## 原故障

Unity 现场表现为：前三点可完成，进入第 4 段后先短暂运动，随后长期零油门直到
600 s 超时。根因不是 SAC 单独“学坏”，而是旧运行架构把在线规划失败、点位专属
动作裁剪和恢复状态机叠在同一个控制循环中：

- 第 4 段把 5 个动作裁成 3 个唯一百分比命令；
- 从 Unity 偏航后的实测姿态用固定 seed 重规划，失败分支绕过通用恢复；
- `PLANNING_DEFERRED` 持续输出零控制，失败计数不终止；
- 异常细节没有进入可追溯遥测。

## 已采用架构

运行时不再规划。地图、13 个原始航点和离线生成的冻结走廊由内容哈希绑定；每个
0.1 s 周期只做一次纯状态转换：输入校验、航点推进、V3 观测、执行器约束、安全
mask、SAC 动作和最终复核。航点切换不停车、不清空 GRU。

控制权边界已经唯一化：

- `NavigationStatus`：唯一命令通道；
- `DeviceStatus`：只读反馈及 ROS 时钟；
- 安全监督器：拥有最终否决权；
- SAC：只在当前可达且预测安全的五动作集合内选择。

旧 PPO/DQN 副本、反向急刹、零净空 profile、在线规划服务、设备双输出桥、vehicle
和 rosbag 辅助链均已退出当前源码。历史模型与日志保留但不会被 V6 加载。

## 进一步发现并修正

1. Windows 时间比 ROS 管理节点慢约 15.5 s，而节点只接受 2 s 内的命令。旧
   `NavigationStatus` 因 timestamp 过期被归零；现改用只读设备时钟。
2. 旧 11→12 走廊为满足全局 0.2 m 净空生成了前进船无法执行的回头弯；经实船极限
   能力确认后，在第 11 点计分圆内选取最大最小净空锚点并改为连续前进曲线。该哈希
   绑定狭窄区使用 0 m 附加净空和 0.4 m 激光急停，离区即恢复 0.2 m／0.6 m。
3. 离线出生扰动原来只验证“当前未碰撞”；现还要求至少一个动作通过同一 2 s 安全
   预测。
4. 离线安全停车原来冻结船体；现用零命令继续传播惯性，与 Unity 行为一致。
5. 静态地图原来每回合重复编译；现每个训练器生命周期只编译一次，单周期实测约
   19 ms，低于 100 ms 预算。
6. 原始 V3 量程曾让 GRU 门控大面积饱和，SAC 温度梯度方向也曾写反；现各物理量
   先按固定尺度归一化再进入 PyTorch `LayerNorm`，温度在熵高于目标时下降，回放
   有界为最近 64 局。
7. 原离线报表把随机分段出生的“到达终点”混同完整 13 点，并且没有输出每局 SAC
   更新指标；现按起始/结束航点统计真实完成数，完整路线曲线独立展示，并记录
   critic/actor loss、alpha、熵和实际更新数。
8. 历史 68 局模型曾被手工写成 `PROMOTED`，但没有 20/20、5 局适应和 5/5 验收
   证据；现 active registry 强制校验全部门控证据，无法再加载该模型。
9. 运行链原来从未调用已有 Unity Reset 服务；现每个计分回合结束后先归零、请求
   Reset 并等待完成确认，失败则停止门控并留下明确遥测。
10. 原奖励会让长航程的逐周期净空/姿态扣分淹没航点奖励；现以进度差分为主，小幅
    正则为辅，并用排序校验保证长程进度和任务完成优于早失败。
11. 非零油门下约 3 s 低速且位移不足 0.05 m 会触发 `MOTION_STALLED`，安全归零后
    进入同一 Unity 复位握手，不再卡到 600 s。
12. 旧离线单局上限为 250 s，会把第 12 点后仍以约 0.108 m/s 正常前进的轨迹误判
    为超时；现与 Unity 契约统一为 600 s。
13. 第 101 局后的 DAgger 取消动作类别重权，actor 沿用统一 Adam 学习率且每局最多 32
    次更新；奇数局采集确定性完整路线，偶数局保留随机恢复样本，critic 始终只学习
    实际执行动作。

## 长期油门与舵角风险

- 五动作必须来自同一次双向 Unity 标定，百分比取整后唯一；任何点位不得再次裁剪。
- 极左和极右不能直接换向，必须经过直行，避免舵机不可达命令和瞬时大变化。
- reward 使用实际油门、舵角及变化率，不使用动作编号距离。
- 常规区 0.2 m 净空和 0.6 m 急停不可学习、不可配置绕过；第 11→12 点唯一狭窄区
  的 0 m／0.4 m 阈值属于地图哈希的一部分，仍禁止实体碰撞。
- 直行 40% 用于建立稳定航速；转向动作使用标定得到的 10% 油门和 5%/10% 舵角。
  是否足够通过全部窄段只能由 Unity 5/5 验收决定，离线动力学不能替代。

## 当前证据门

全新 V6 已在第 400 个训练回合后通过 20/20 离线完整路线评估：20 局均完成 13 点，
零碰撞、零超时、零 `NO_SAFE_ACTION`。当前阶段为 `UNITY_ADAPT`，仍须完成 5 局
Unity 适应和连续 5/5 Unity 冻结验收。最终五局未完成前，结论仍只能是“架构与
控制链已修复，模型仍在 Unity 门控中”。
