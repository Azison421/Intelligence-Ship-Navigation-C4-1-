# 海航赛智能船导航算法审查总结

审查日期：2026-08-04  
工作区：`G:\NavAlg - 副本`  
目标：固定 `National_Test` 地图、13 个静态目标点、已知静态障碍；以能在样例
入口中稳定跑通为优先，主架构采用 Informed RRT* 实现 + SAC，允许局部点位采用
确定性算法。

## 1. 结论

原审查文档的工程风险方向有一部分正确，但它混合了旧代码状态、与本次固定地图
目标冲突的泛化建议，以及未经当前工作区验证的结论。按本次重新审查后的判断：

1. **原 v37 基线配置的 13 点离线闭环已跑通。** 每个目标点的最小距离均不超过
   0.5 m，抵达顺序严格递增，点 11 的安全出口释放发生在抵达点 11 之后、抵达点
   12 之前，整回合无安全截断和超时；该证据不自动转移给新的零净空配置。
2. **本次真正复现到的故障在第 11 点入口。** 原实现把“入点 + 倒车退出”的整个
   复合轨迹视为确定性开环动作，船在距目标 0.5846 m 时所有候选预测碰撞，停止于
   `NO_SAFE_ACTION:MOTION_COLLISION`，只完成 10/13。
3. **修复采用局部确定性闭环算法。** 点 11 入点阶段根据实时状态跟踪轨迹并生成
   五个安全候选，但不调用 SAC；只有进入原始 0.5 m 目标圆后才锁定倒车出口。
   这符合“局部点位可用其他算法、目标是跑通”的要求。
4. **样例入口和 UI 协议未改。** 三个位置参数、`DQN_NAV` 名称和
   Reset／自动模式／路线／Train 流程保留。CLI 无参数运行进入 `unity_test` 混合
   自训练，并从活动冠军或 v37 零净空保守种子热启动；显式 checkpoint、显式模式、
   `--validate-only` 和程序化入口默认值不启动梯度更新。
5. **目前只能判定离线通过，不能判定比赛实跑通过。** 本次没有启动 Unity、ROS
   或 MATLAB。用户截图先后表明旧零净空候选在第 4→5 点撞到中间浮标、上一版
   慢转候选持续 `PLANNING_DEFERRED`；当前保守控制与规划延迟重试尚未在 Unity 中复测。
6. **当前仓库还没有由真实 Unity 验收产生的 v5 晋级 checkpoint。** 旧 v37 候选已完成一次
   13/13 聚焦离线回放；当前默认候选是相同权重的零净空、保守 3→4→5 Unity
   实验副本，
   尚未做 13/13 零净空回放。相关候选 readiness 都为 false；`live` 模式失败关闭是
   正确行为。
7. **主算法、初始权重、13 点和路线代价保持不变，但 Train 现会迭代 SAC 权重。**
   安全 profile 仍为地图额外净空 0 m、激光提前急停 0 m；新增机动 profile 把
   第 3→4 段限制为 `油门≤0.1、|舵角|≤0.1`，并把第 4→5 点固定转弯改为
   `Control(0.1, 0.12)`。规划延迟时会归零并在同一回合继续重试，不再按连续次数 Reset。
   0.4 m 船体碰撞外形、预测碰撞和数据失效保护保留；单回合上限仍为 5000 步／
   600 s，`MAX_EPOCH` 不变。每代候选使用不可覆盖 v5 文件保存，只有通过严格
   20/20 离线与 5/5 Unity 门槛及冠军比较后才更新活动指针。

本报告中的“13 点进出”定义为：点 1–12 进入目标圆后还能安全抵达下一点；点 13
进入目标圆后结束任务并保持零控制。终点没有为了形式上的“出点”继续航行。

## 2. 比赛资料与地图解释

官方资料显示任务运行在 Unity、Simulink 和 ROS 组成的平台上，目标是使用深度
强化学习完成避障与目标到达；评分关注成功率、完成时间和路径长度：

- [Maritime Intelligent Navigation 2026](https://spaitlab.github.io/Maritime-Intelligent-Navigation-2026/)
- [赛事资源](https://spaitlab.github.io/Maritime-Intelligent-Navigation-2026/%E8%B5%9B%E4%BA%8B%E8%B5%84%E6%BA%90/)
- [操作手册](https://spaitlab.github.io/Maritime-Intelligent-Navigation-2026/%E6%93%8D%E4%BD%9C%E6%89%8B%E5%86%8C/)
- [评分细则](https://spaitlab.github.io/Maritime-Intelligent-Navigation-2026/%E8%AF%84%E5%88%86%E7%BB%86%E5%88%99/)

用户对 `.codex/memory/download.png` 的说明作为本次地图判读的权威输入：

- 黄色点：13 个正式目标点；
- 白线：目标点连接关系，不是已验证轨迹；
- 红白相间物体：静态障碍；
- 船从南侧，即图片下方进入；
- 图片上北、下南、左西、右东。

代码中的局部 ENU 坐标与此一致：`x` 向东、`y` 向北。旧 v37 基线的静态 sidecar
额外净空为 0.2 m；零净空实验为 0 m。目标到达半径仍为 0.5 m，船体碰撞外形仍为
0.4 m。

## 3. 全文件审查范围与本次增量复核

审查排除了 `.git`、`.venv`、pytest/cache 和生成的 `__pycache__`，其余 354 个
本地文件全部进入机器清单与格式／完整性检查：

| 类型 | 数量 | 检查方式与结果 |
|---|---:|---|
| Python | 105 | 全部 AST 解析；0 个语法错误；活动运行链再做人工语义审查 |
| JSON | 90 | 全部 UTF-8 解析；0 个解析错误；地图、manifest、日志按 schema 抽查 |
| Markdown | 24 | 全部检查；5 个项目文档已统一，`.codex/skills` 说明仅审阅不改写 |
| checkpoint `.pt` | 35 | 33 个有 manifest 且文件哈希无失败；2 个为诊断／旧 DQN 无 manifest |
| 图片 | 12 | 文件签名检查；Unity 地图和历史运行截图人工查看 |
| TOML | 9 | 全部解析；0 个解析错误 |
| DOCX／PDF | 各 1 | 容器或文件签名有效；没有把二进制存在误当成算法验证 |
| 日志 | 64 | 结构与关键失败记录检索；没有用旧日志替当前代码背书 |

还发现：

- 项目文档不存在失效的相对链接；3 个断链均位于本地 ADR skill 的示例占位链接，
  不属于产品文档。
- `usvlib4ros（1）` 是被 Git 跟踪的旧源码副本，包含旧 DQN 和重复文件。没有得到
  删除授权，因此本次不动它；正式入口只审查和使用 `usvlib4ros/`。
- `artifacts/checkpoints/national_test_sac_v32_diagnostic.pt` 与旧副本中的
  `dqn/a.pt` 没有 manifest；二者不能进入当前正式加载链。
- `setup.py` 的运行依赖为空，且没有地图 package data；wheel 交付仍不完整。

“全部文件审查”在这里表示对所有文件做清单、可解析性、签名、hash／manifest 和
敏感模式检查，再对活动代码链、地图、checkpoint、测试及关键历史日志做语义审查；
不表示把 PyTorch 二进制权重或第三方 skill 文本反编译成人工逐行证明。

上表是原审查快照；零净空和本次点 4→5 低速增量都没有重跑全量文件解析或 pytest。
本次自训练增量排除 `.git`、`.venv`、`.codex`、缓存和第三方依赖后，共检查 11 份
项目 Markdown（含新增 TDD 报告）；更新范围只涉及当前入口、自训练、日志、晋级、
安全距离、局部点 3–5 控制、规划延迟重试、5000 步／600 s 和验证状态。历史 TDD
报告保留原结论，并由新报告标明后续状态。

## 4. 当前活动运行链

```text
usvlib4ros/main.py
  -> USVNavMain.start(host, port, deviceId)
  -> DQN_NAV
  -> FixedMapNavigationService
       -> SelfTraining 状态机（仅无参数入口）
       -> FixedMapSACTrainer（95 个离线训练 + 20 个离线评估）
       -> FixedMapControllerCore（Unity 探索／验收／冠军运行）
       -> 固定地图／13 点任务适配
       -> KinodynamicInformedRRTStarPlanner
       -> Recurrent Discrete SAC（普通航段）
       -> 局部确定性复合机动（特殊点位）
       -> PredictiveSafetySupervisor（最终动作裁决）
  -> 样例 ROS/Unity 控制与 UI 状态
```

安全监督器拥有最终控制权。任一局部算法提出的动作都要经过地图运动碰撞预测；没有
安全候选时归零并停止。任务点与规划轨迹是两层不同数据：规划门、handoff 或 bypass
只能帮助生成可行轨迹，不能替代进入原始 0.5 m 任务圆。

## 5. “Informed RRT* + SAC”的准确状态

仓库确实实现并调用 `KinodynamicInformedRRTStarPlanner`，包括动力学传播、informed
sampling、rewiring、地图快照版本和轨迹重放验证。普通航段由该规划器生成轨迹，SAC
在五个前进动作中提出局部控制，再由安全掩码裁决。

但 `plan_fixed_leg()` 和 `plan_fixed_route()` 当前默认：

```python
optimize_with_rrtstar = False
```

它会把 `stop_on_first_solution` 设为 true，因此若 grid/lattice warm start 或直接
connector 已通过动力学与碰撞验证，就立即返回，不会继续花剩余预算做 informed
sampling 与 rewiring。这个配置更适合当前“固定地图优先跑通、避免在线规划超时”的
目标，但不能宣传成“每个航段都执行了完整 Informed RRT* 最优化”。

当前主方案与备选关系为：

| 方案 | 当前用途 | 权衡 |
|---|---|---|
| 首个可行动力学 seed + SAC | 默认运行方案 | 延迟低、已离线跑通；路径不保证 RRT* 渐近优化 |
| `optimize_with_rrtstar=True` | 可选离线对比 | 能继续 informed sampling/rewiring；尚未验证在线时延与 13 点稳定性 |
| 局部确定性闭环／primitive | 点 4–6、11–13 | 针对窄区稳定；只服务固定地图，不宣称泛化 |

本次没有为了算法名称强行打开完整优化，因为那会扩大时延和回归范围，且不是修复
第 11 点进出失败所必需。

## 6. 13 点几何与控制分工

下面坐标是 sidecar 原点归一后的米制 ENU。`中心净空` 是目标中心相对船体接触边界
的静态地图净空；旧基线要求 0.2 m，当前 Unity 实验要求 0 m。点 6 和点 11 的中心
小于旧基线要求，但各自规划 gate 位于正式 0.5 m 目标圆内。到达判定和表中坐标
没有改变；当前默认候选只调整点 3 后至点 5 的局部速度与转弯控制。

| 点 | x 东 | y 北 | 中心净空 | 当前进入／退出策略 |
|---:|---:|---:|---:|---|
| 1 | 38.815 | 71.006 | 2.062 | 普通规划 + SAC；抵达点 2 证明已退出 |
| 2 | 38.917 | 74.908 | 0.876 | 普通规划 + SAC；抵达点 3 证明已退出 |
| 3 | 40.152 | 77.859 | 0.744 | 普通规划 + SAC；抵达后先制动，再以油门和舵角绝对值均不超过 0.1 进入点 4 复合航段 |
| 4 | 41.001 | 81.096 | 0.901 | 低速先过非计分 approach，再进原目标，持续到西侧 handoff |
| 5 | 40.715 | 84.376 | 0.630 | handoff 后以 `Control(0.1, 0.12)` 固定转弯进入，随后接局部出口 |
| 6 | 39.692 | 87.526 | 0.129 | gate `(39.500, 87.500)` + primitive 局部搜索，进入后朝点 7 退出 |
| 7 | 38.374 | 91.033 | 0.751 | 普通规划 + SAC，避免直线贴障 |
| 8 | 35.944 | 93.089 | 0.788 | 普通规划 + SAC，规划轨迹替代白色直连线 |
| 9 | 34.336 | 95.851 | 1.468 | 普通规划 + SAC |
| 10 | 32.497 | 99.441 | 0.922 | 普通规划 + SAC，为点 11 窄入口对齐 |
| 11 | 29.038 | 99.810 | 0.125 | gate `(29.100, 99.500)`，确定性闭环入点；随后沿入口倒车到东侧释放 |
| 12 | 26.266 | 97.132 | 1.439 | 点 11 出口后向南绕障并转西，确定性轨迹进入／离开 |
| 13 | 23.745 | 92.054 | 2.080 | 终点 primitive 进入 0.5 m 圆，完成后保持零控制 |

白色连接线不能直接作为轨迹：几何检查中，若从上一点直连，点 5、8、10 的航段会
穿入旧基线障碍膨胀区，点 6、7、11、12 的最小净空也低于原 0.2 m 要求。零额外
净空不会关闭船体接触判断，因此仍需要动力学规划、局部复合轨迹和安全监督器。

## 7. 第 11 点故障复现与修复

### 修复前

点名离线用例在修复前得到：

```text
completed=False
safety_stop=True
steps=1883
mission_index=10
point_11_min_distance=0.584636 m
stop_reason=NO_SAFE_ACTION:MOTION_COLLISION
```

零基 `mission_index=10` 即第 11 点。旧逻辑只要轨迹被标成 narrow composite，
就用固定中间动作并保持旧 hidden state。入口误差无法闭环消除，又没有让 SAC 或
局部控制器调整；距目标尚差约 8.5 cm 时，所有等价候选都预测碰撞。

### TDD 约束

先增加以下失败用例：

```text
test_runtime_executes_closed_loop_narrow_ingress_without_calling_sac
```

测试用真实点 11 窄区轨迹，把 `policy.act` 替换为一旦调用就抛异常。修复前它失败，
证明入口会进入 SAC；最终要求同时满足：

- 入点阶段不调用 SAC；
- 每步根据当前实测状态重新计算 nominal feedback；
- 五个候选都由安全预测检查；
- 在安全候选中选择最接近 nominal 的最小干预动作；
- 只有真正进入点 11 的 0.5 m 圆后才切到锁定倒车出口。

训练器和运行时使用相同分支，避免离线闭环与正式控制逻辑分叉。路线指导契约同步
升级到 `national-test-reversible-composite-v37`，旧 checkpoint 会失败关闭。

## 8. 原审查文档逐项复核

| 原判断 | 当前结论 | 状态／处理 |
|---|---|---|
| `tests/` 会被主程序运行 | 不会；但测试用于锁定入口契约和安全行为 | 测试保留，部署可按规则排除 |
| CLI 无参数入口允许测试候选 | 当前有意选择 v37 零净空保守 3→4→5 `unity_test` 供 Unity 观察 | 新候选仅允许 `unity_test`；显式 `live` 仍失败关闭，程序化入口默认仍为 `live` |
| 缺少资产 preflight | 对旧快照成立 | 审查开始前已有本地修改，连接前检查资产 |
| 成功后继续 Reset 下一回合 | 对旧快照成立 | 审查开始前已有本地修改，正式模式单回合并保持零控制 |
| 固定地图／13 点耦合是错误 | 与本次明确范围冲突 | 固定北湖地图是有意约束，不做随机地图泛化 |
| 应融合动态障碍／任意点数 | 可能是未来需求，不是本任务 | 本次不实现，避免扩大范围 |
| 规划时延可能影响成绩 | 风险仍存在 | 默认早停求可行解；完整优化需另做延迟对比 |
| 模块较大、特殊逻辑耦合 | 成立 | 属于维护债务；本次只做局部修复，不大重构 |
| 传感器新鲜度依赖对象替换 | 成立，当前回调模式下未复现 | 保留风险，未来改接收时间戳／序列号 |
| daemon／退出生命周期不完整 | 对旧快照成立 | 审查开始前已有 stop、join、零控制与 signal 清理修改 |
| 打包元数据不完整 | 成立 | 未修复；源码运行是当前唯一受支持方式 |
| README 与代码不一致 | 成立 | 本次已统一三个项目说明文件并移除旧参数／敏感示例 |
| 应删除 `usvlib4ros（1）` | 有清理价值，但属于破坏性范围变更 | 未获授权，不删除，仅明确非正式入口 |

原文最重要的问题不是“测试目录能不能删”，而是不同证据层级被混在一起。现在必须
严格区分：静态审查、纯 Python 离线闭环、Unity 候选测试和正式 `live` 晋级。

## 9. checkpoint 与历史 Unity 证据

程序化／显式 `live` 未指定路径时的默认文件：

```text
artifacts/checkpoints/national_test_sac_live_v10_tested.pt
```

其 manifest 是 schema v4，但 `offline_ready=false`、`live_ready=false`，路线指导
早于 v37。旧基线候选仍保留为：

```text
artifacts/checkpoints/national_test_sac_v37_unity_test.pt
```

旧 manifest 没有安全或机动 profile 字段，运行时自动回退到原 0.2 m 地图净空、
0.6 m 激光提前急停阈值和 `0.4/0.2` 点 4→5 转弯。旧零净空候选仍保留为：

```text
artifacts/checkpoints/national_test_sac_v37_zero_clearance_unity_test.pt
```

上一版慢转候选也保留为：

```text
artifacts/checkpoints/national_test_sac_v37_zero_clearance_slow_turn_unity_test.pt
```

CLI 无参数运行现在另行选择：

```text
artifacts/checkpoints/national_test_sac_v37_zero_clearance_conservative_345_unity_test.pt
```

四份 `.pt` 的权重 SHA-256 都是
`ba9243d5db3d3375a974373e48f617308b37bce7712c233288a034c0f5beb271`。当前独立 manifest
记录 0 m／0 m、点 3→4 的 `0.1/±0.1` 包络、点 4→5 控制 `0.1/0.12`、
`unity_test_only=true`，以及零净空地图 hash
`ed04ab551655edbe8a104e392201695360a9cf01044a2cccfbc9535839da9f57`；路线 hash
保持不变。它的 `offline_ready`、`live_ready` 都为 false，尚未产生 Unity 或 13/13
零净空回放证据。旧慢转 manifest 缺少舵角上限字段时回退为 1.0，保持原行为；显式
`live` 会拒绝新的安全和机动 profile。这不是绕过正式晋级门，而是把受限 Unity
实验入口与正式入口分开。

历史日志 `artifacts/logs/unity-candidate-20260803-140347.json` 记录了：

```text
passed=false
completed_waypoints=4
collision_count=1
stop_reason=CONFIRMED_COLLISION
```

该运行发生在本次第 11 点入口修复以及当前点 4–6 指导契约之前，只能说明历史候选
失败，既不能否定当前离线修复，也不能证明当前代码已能在 Unity 跑通。下一次 Unity
验证必须产生新的日志、截图和路线／模型契约 hash。

## 10. 离线验证证据

严格遵守用户要求，本次没有运行全量回归。原 v37 基线证据使用的核心点名用例为：

| 用例 | 结果 | 证明边界 |
|---|---|---|
| `test_fixed_map_sac_trains_complete_safe_episode_and_saves_checkpoint` | PASS | 简化动力学下 13/13、顺序、点 11 出口、净空和 v37 manifest |
| `test_runtime_executes_closed_loop_narrow_ingress_without_calling_sac` | PASS | 点 11 入口闭环且 SAC 不接管 |
| `test_runtime_executes_safe_reverse_escape_without_calling_sac` | PASS | 点 11 入点后的确定性倒车出口 |
| `test_runtime_preserves_point_four_composite_until_safe_handoff` | PASS | 点 4 进点后不提前丢弃复合轨迹 |
| `tests/test_sample_entry_compatibility.py` | 点名文件验证 | 样例入口、模式、preflight、UI 和停止契约 |

13 点主用例新增了以下明确断言：

- `mission_index == 13`；
- 13 个 `waypoint_min_distances_m <= 0.5`；
- 13 个 `waypoint_reached_steps` 均存在且严格递增；
- `point_11_reached < narrow_escape_release < point_12_reached`；
- 全回合最小净空大于地图要求。

这些证据不覆盖 ROS 网络、Unity 真实动力学、激光雷达时序、Simulink 或比赛结算。

本次零净空／5000 步增量另以 TDD 增加并点名运行：

| 用例 | 结果 | 证明边界 |
|---|---|---|
| `test_runtime_safety_profile_keeps_legacy_defaults_and_parses_zero` | PASS | 旧 manifest 回退 0.2/0.6，新 profile 解析 0/0，负值失败关闭 |
| `test_zero_clearance_map_removes_buffer_but_keeps_footprint_collision` | PASS | 0–0.2 m 状态放行、船体接触仍拒绝、地图 hash 分离 |
| `test_zero_clearance_runtime_only_stops_laser_at_contact` | PASS | 正激光距离不提前急停，0 m 接触停止 |
| `test_zero_clearance_candidate_reuses_v37_weights_and_is_unity_only` | PASS | 权重 SHA 相同、新候选门禁、旧 v37 基线兼容 |
| `test_unity_episode_uses_5000_step_and_600_second_limits` | PASS | 模拟时间下执行满 5000 步、默认墙钟 600 s；连续 3 次规划延迟后第 4 次仍可完成，不按次数复位 |
| `test_bounded_episode_tools_use_600_second_limit` | PASS | 两个单回合辅助入口与主循环统一为 600 s |
| `test_report_logger_records_5000_steps_with_automatic_svg_scale` | PASS | CSV 记录 5000，SVG 按数据自动缩放 |
| `test_promotion_requires_three_matching_passed_unity_validation_logs` | PASS | 599 s 合格 Unity 证据不再被旧 300 s 门槛拒绝 |
| `test_runtime_maneuver_profile_keeps_legacy_defaults_and_parses_slow_turn` | PASS | 旧 manifest 回退原局部控制，新 profile 解析油门／舵角包络，非法值失败关闭 |
| `test_points_three_to_five_conservative_profile_limits_controls_and_speed` | PASS | 点 3→4 油门和舵角绝对值不超过 0.1，点 4→5 使用 0.12 舵角并保持连续无碰撞 |

以上只是聚焦离线契约验证；没有为当前保守候选补跑 13/13，也没有启动 Unity。

## 11. 本次修改与既有本地改动的边界

审查开始时工作树已不是 clean。以下文件已有用户／前序本地修改，本次不把其成果
冒认为新编写，也没有回退：

```text
tests/test_sample_entry_compatibility.py
tools/run_candidate_live_episode.py
usvlib4ros/main.py
usvlib4ros/navigation/fixed_map_service.py
usvlib4ros/policy/checkpoint_promotion.py
usvlib4ros/user/nav.py
```

这些既有修改包含显式 `PolicyMode`、资产 preflight、单回合、reset 握手和清洁停止。

本次直接修改集中在：

```text
usvlib4ros/navigation/fixed_map_runtime.py
usvlib4ros/navigation/training_reports.py
usvlib4ros/policy/fixed_map_trainer.py
usvlib4ros/planning/fixed_route.py
tests/test_fixed_map_runtime.py
tests/test_fixed_map_trainer.py
tests/test_training_reports.py
README.md
usvlib4ros_main_README.md
readme
reports/README.md
reports/training_logging.tdd.md
海航赛智能船导航算法审查总结.md
.gitignore
```

本次零净空、点 4→5 慢转及点 3→5 保守控制／规划延迟重试增量在上述既有工作之上
进一步修改／新增：

```text
usvlib4ros/navigation/fixed_map_runtime.py
usvlib4ros/navigation/fixed_map_service.py
usvlib4ros/planning/fixed_route.py
usvlib4ros/policy/checkpoint_promotion.py
usvlib4ros/policy/fixed_map_trainer.py
usvlib4ros/main.py
tools/run_candidate_live_episode.py
tools/run_one_live_episode.py
tests/test_checkpoint_promotion.py
tests/test_fixed_map_runtime.py
tests/test_fixed_map_trainer.py
tests/test_national_route_planner.py
tests/test_sample_entry_compatibility.py
tests/test_training_reports.py
artifacts/checkpoints/national_test_sac_v37_zero_clearance_unity_test.pt
artifacts/checkpoints/national_test_sac_v37_zero_clearance_unity_test.pt.json
reports/zero_clearance_5000_steps.tdd.md
artifacts/checkpoints/national_test_sac_v37_zero_clearance_slow_turn_unity_test.pt
artifacts/checkpoints/national_test_sac_v37_zero_clearance_slow_turn_unity_test.pt.json
reports/point4_point5_slow_turn.tdd.md
artifacts/checkpoints/national_test_sac_v37_zero_clearance_conservative_345_unity_test.pt
artifacts/checkpoints/national_test_sac_v37_zero_clearance_conservative_345_unity_test.pt.json
reports/points3_to5_conservative_reset.tdd.md
相关项目 Markdown
```

本次 SAC 自训练增量进一步修改／新增：

```text
usvlib4ros/policy/self_training.py
usvlib4ros/policy/recurrent_sac.py
usvlib4ros/policy/fixed_map_trainer.py
usvlib4ros/navigation/fixed_map_runtime.py
usvlib4ros/navigation/fixed_map_service.py
usvlib4ros/navigation/training_reports.py
usvlib4ros/main.py
usvlib4ros/user/nav.py
tests/test_self_training.py
tests/test_recurrent_sac.py
tests/test_fixed_map_trainer.py
tests/test_fixed_map_runtime.py
tests/test_sample_entry_compatibility.py
tests/test_training_reports.py
reports/self_training.tdd.md
相关项目 Markdown 与 .gitignore
```

没有创建提交，也没有清理旧源码副本或改写用户已有 checkpoint。

## 12. 当前放行状态与下一步

| 层级 | 状态 | 说明 |
|---|---|---|
| 全文件静态清单／解析 | PASS | 非环境文件已清点；解析和 hash 未见阻断错误 |
| 13 点纯 Python 离线闭环 | PASS | 当前固定地图和简化动力学下完成 |
| 样例入口兼容 | PASS（点名测试范围） | 未做全量回归 |
| Train CSV/SVG 日志 | PASS（点名测试范围） | 可记录 5000 步并自动缩放；真实 Unity 数据待产生 |
| SAC 自训练状态机／续训／更新 | PASS（聚焦离线测试） | 95/5/20/5、16/80 更新、6:2 回放、原子状态和 v5 门禁已隔离验证 |
| 旧 v37 基线候选 | OFFLINE REPLAY PASS／NOT PROMOTED | 原 0.2/0.6 配置完成一次 13/13 回放；readiness 仍为 false |
| 上一版 v37 零净空 Unity 候选 | UNITY OBSERVATION FAILED／NOT PROMOTED | 用户截图显示第 4→5 点碰撞；候选保留供对照 |
| 上一版零净空慢转候选 | UNITY OBSERVATION FAILED／NOT PROMOTED | 用户终端显示持续 `PLANNING_DEFERRED`；显式路径保留供对照 |
| 当前零净空保守 3→4→5 候选 | FOCUSED CONTRACT PASS／NOT PROMOTED | 同权重、0/0 安全 profile、小油门／小舵角和规划延迟重试契约已验证；未做 13/13 或 Unity 复测 |
| Unity 闭环 | NOT VERIFIED FOR CURRENT CANDIDATE | 当前修复后未运行；等待用户复测 |
| ROS／Simulink 联调 | NOT VERIFIED | 本次未连接 |
| 正式 `live` | BLOCKED UNTIL REAL GATES PASS | 必须由真实 20/20 离线与 5/5 Unity 证据生成并激活兼容 v5；当前种子仍被拒绝 |

因此当前代码可以交给用户进行下一次 Unity 定点复测，但不能宣称已达到正式比赛
放行条件。建议复测时每次只跑一回合，并保留：

1. 出问题时的目标点编号和 `mission_index`；
2. 终端中 `reason`／`stop_reason`；
3. 失败前 5–10 秒的船位、航向、速度和控制输出；
4. Unity 截图或短视频，画面需包含目标点与障碍；
5. 本次 checkpoint manifest 和生成的 Unity 日志，但不要公开连接主机或设备 ID。

拿到新的 Unity 反馈后，应只补对应点位的回放和点名测试，不先运行全量回归。

## 13. Train 按钮日志补充

Unity“开始训练”每次激活时创建独立 `run_id`；每个 episode 结束后，程序在项目
`reports/` 里累计保存：

- `training_runs.csv`：多次点击的边界和成功 episode 数；
- `training_episodes.csv`：每回合总步长及成功到终点的总步长；
- `waypoint_steps.csv`：13 个点的累计步长、分段步长和最小距离；
- 3 张对应的灰度 SVG 曲线。

未抵达点记录为空并在曲线上断开。报告写入失败只记录错误，不改变零控制、安全监督
或 UI 状态机。本文最初记录的旧 3 份 CSV／3 张 SVG 继续兼容；无参数入口的 Train
现已接入实际 SAC 混合训练，并新增独立自训练 CSV／SVG。详细字段和 TDD 证据分别见
`reports/README.md`、`reports/training_logging.tdd.md` 与 `reports/self_training.tdd.md`。

当前每回合默认最多记录 5000 个控制步或 600 s。SVG 始终按累计实际数据自动缩放，
5000 只是本次运行上限，不是曲线的硬编码纵轴上限。

## 14. SAC 自训练与自动迭代补充

无参数 `main.py` 在 Train 触发后使用持久化状态机：首次先运行 5 个确定性 Unity
冠军基线回合；每代随后运行 95 个 Python 离线训练回合、5 个 Unity 安全探索回合、
20 个确定性离线评估回合和 5 个独立 Unity 确定性验收回合。训练回合累计目标从
1000 开始，完成后发布零控制等待；下一次点击把目标增加 1000。

离线回放和 Unity 回放分别保留 32／20 个完整回合。每个离线回合做 16 次 SAC
更新，Unity 探索块结束后做 80 次更新；有 Unity 数据时每批 8 条按 6:2 混合，
否则使用 8 条纯离线样本。网络、目标网络、优化器、温度、训练步、Python／Torch
随机状态、双回放池及阶段游标在每个完整回合后原子保存。停止中的半回合被丢弃，
`PLANNING_DEFERRED` 仍只归零重试，不作为新增复位条件。

候选策略可以在全路线选择任一通过预测安全掩码的动作；点 3→4 与点 4→5 的 5 个
候选仍受 `0.1/±0.1` 和 `0.1/±0.12` 硬包络约束。0.4 m 船体、碰撞预测、传感器
新鲜度和安全监督最终否决权没有解除。碰撞回合写入终止负样本并使当代失去晋级
资格；代末仍生成不可覆盖的 v5 证据文件，然后恢复最近冠军继续下一代。

自动晋级要求离线 20/20 和 Unity 5/5 全部完成，且零碰撞、零激光急停、零安全停止、
零超时、零未恢复不安全事件。完成率优先；完成率相同时，候选 Unity 中位总步数需
至少改善 2%。通过后才原子更新 `national_test_sac_active.json`，并在下一回合加载；
显式 `--checkpoint PATH` 永远固定。上述逻辑仅经聚焦离线测试，尚未由本次真实 Unity
运行验证，证据见 `reports/self_training.tdd.md`。
